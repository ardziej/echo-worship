section

each verse, i in el.song.prompterSequence
    section(data-state='song-top')
        each part, index in verse
            if index === 'text'
                each text, j in part
                    span.song-block
                        if text
                            span.song-element=text
                        else
                            span

section.full-screen